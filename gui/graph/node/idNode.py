class IdNode:
    """
        Class usefull to identify a node with the nodeName, and the nodeType.

        Not used yet.
    """

    def __init__(self, nodeName, nodeType):
        #self._graph = graph
        self._node = node
        self._clip = clip
